name = "whoistrs-worker"
main = "index.js"
compatibility_date = "2024-01-01"

[triggers]
crons = ["*/10 * * * *"]

# Environment variables — set secrets via `wrangler secret put` or the Cloudflare dashboard.
# Never put ANTHROPIC_API_KEY or BACKEND_URL values in this file.
